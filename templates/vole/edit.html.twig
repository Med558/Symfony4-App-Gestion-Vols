{% extends 'adminlayout.html.twig' %}


{% block body %}
    <h1>Edit Vole</h1>

    {{ include('vole/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('vole_index') }}">back to list</a>

    {{ include('vole/_delete_form.html.twig') }}
{% endblock %}
